{{- if .Values.conductor.enabled  }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.conductor.service.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: conductor-svc
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: conductor
    app.kubernetes.io/part-of: mender
    helm.sh/chart: "{{ .Chart.Name }}"
{{- with .Values.conductor.service.annotations }}
  annotations: {{ tpl (toYaml .) $ | nindent 4 }}
{{- end }}
spec:
  type: {{ .Values.conductor.service.type }}
  {{- if and (eq .Values.conductor.service.type "ClusterIP") .Values.conductor.service.clusterIP }}
  clusterIP: {{ .Values.conductor.service.clusterIP }}
  {{- end }}
  {{- if and (eq .Values.conductor.service.type "LoadBalancer") .Values.conductor.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.conductor.service.loadBalancerIP }}
  {{- end }}
  {{- if .Values.conductor.service.externalIPs }}
  externalIPs: {{ toYaml .Values.conductor.service.externalIPs | nindent 4 }}
  {{- end }}
  {{- if .Values.conductor.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges: {{- toYaml .Values.conductor.service.loadBalancerSourceRanges | nindent 4 }}
  {{- end }}
  ports:
  - port: {{ .Values.conductor.service.port }}
    protocol: TCP
    targetPort: 8080
    {{- if .Values.conductor.service.nodePort }}
    nodePort: {{ .Values.conductor.service.nodePort }}
    {{- end }}
  selector:
    app.kubernetes.io/name: conductor
{{- end }}
